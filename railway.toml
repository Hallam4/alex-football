[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["python", "node"]
buildCmd = "cd frontend && npm install && npm run build && cd ../backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd backend && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
